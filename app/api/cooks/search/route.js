// Placeholder — connects to Supabase when credentials are configured
// See backend/src/routes/cooks.ts for full implementation
export async function POST(request) {
  return Response.json({ success: true, data: [], count: 0 });
}
